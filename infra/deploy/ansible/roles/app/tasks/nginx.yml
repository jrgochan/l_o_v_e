---
- name: Deploy Nginx Config
  template:
    src: nginx.conf.j2
    dest: /etc/nginx/conf.d/love.conf
    mode: '0644'
  notify: Reload Nginx

- name: Test Nginx Config
  command: nginx -t
  changed_when: false
