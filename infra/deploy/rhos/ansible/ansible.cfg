[defaults]
# Use an explicit inventory file instead of directory walking
inventory = inventory.ini
deprecation_warnings = False
# Pin interpreter to the Python that has the kubernetes library installed
interpreter_python = /Applications/Xcode.app/Contents/Developer/usr/bin/python3

[callback_defaults]
result_format = yaml

[connection]
pipelining = True
