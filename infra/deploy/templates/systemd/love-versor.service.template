[Unit]
Description=Love Stack - Versor API
After=network.target postgresql-15.service redis.service

[Service]
User=${USER}
Group=${GROUP}
WorkingDirectory=${APP_DIR}/versor
ExecStart=${APP_DIR}/versor/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port ${PORT_VERSOR}
Restart=always
EnvironmentFile=${APP_DIR}/infra/deploy/configs/env/production.env

[Install]
WantedBy=multi-user.target
