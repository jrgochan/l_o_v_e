import { AnimationModeSelector } from '../../../../../components/admin/panels/ControlPanel/AnimationModeSelector';

describe('AnimationModeSelector', () => {
    it('should be defined', () => {
        expect(AnimationModeSelector).toBeDefined();
    });
});
