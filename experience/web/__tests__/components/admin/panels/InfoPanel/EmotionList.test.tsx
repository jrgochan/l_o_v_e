import { EmotionList } from '../../../../../components/admin/panels/InfoPanel/EmotionList';

describe('EmotionList', () => {
    it('should be defined', () => {
        expect(EmotionList).toBeDefined();
    });
});
