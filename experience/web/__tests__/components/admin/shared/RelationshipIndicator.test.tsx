import { RelationshipIndicator } from '../../../../components/admin/shared/RelationshipIndicator';

describe('RelationshipIndicator', () => {
    it('should be defined', () => {
        expect(RelationshipIndicator).toBeDefined();
    });
});
