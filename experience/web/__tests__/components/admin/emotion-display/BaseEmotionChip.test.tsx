import { BaseEmotionChip } from "../../../../components/admin/emotion-display/BaseEmotionChip";

describe("BaseEmotionChip", () => {
  it("should be defined", () => {
    expect(BaseEmotionChip).toBeDefined();
  });
});
